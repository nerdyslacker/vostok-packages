case "${ACTION}" in
pre)
    ;;
post)
    # Print instructions for enabling the service
    echo
    echo "To enable the plasmazones service, run the following command as root:"
    echo "  ln -s /etc/sv/plasmazonesd /var/service/"
    echo "This will start the service via runit."
    ;;
esac